{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"/c/Users/lucca/OneDrive/Área de Trabalho/IA/Fluxo de Produção\" && git add -A && git diff --cached --quiet || (git commit -m \"auto: update via Claude Code\" && git push) 2>/dev/null || true",
            "statusMessage": "Sincronizando com GitHub..."
          }
        ]
      }
    ]
  }
}
